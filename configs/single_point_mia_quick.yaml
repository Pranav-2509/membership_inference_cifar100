# Quick single-point MIA (few iterations, small subset) for debugging.
# Dataset: CIFAR-100.

target_index: 0
num_classes: 100
n_iterations: 3
subset_size: 200

epochs: 2
batch_size: 32
lr: 0.01

alpha: 0.05
save_plots: true
seed: 42
